import 'package:flutter/material.dart';

/// Colores globales REXI (tema y componentes en `core/widgets`).
abstract final class AppColors {
  static const Color seed = Color(0xFF0D9488);
  static const Color shellBackground = Color(0xFFF5F7F9);
}
